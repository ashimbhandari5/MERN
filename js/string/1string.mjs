/* 'ashim'
"ashim"
`ashim`

string can be dfined in three ways

we cannot use single quotes inside single quotes
we cannot use double quotes inside double quotes
we cannot use backtick quotes inside backtick quotes
 */ console.log(object);
