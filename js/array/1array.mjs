let colors = ["red", "green", "blue"];
console.log(colors[2]);

let ashim = ["bhandari", 5, true];
console.log(ashim[0]);
console.log(ashim);

//change
ashim[0] = "rai";
ashim[1] = 50;
console.log(ashim);

// delete array element

delete ashim[1];
console.log(ashim);

//key points
/*
-Array help organize multiple values
 under a single name.
-Indexing starts from 0,which means the
first element is at position 0.
- we can easily access and modify array
or add items to an array.
 */
